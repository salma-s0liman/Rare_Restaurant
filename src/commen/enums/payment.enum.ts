
export enum paymentMethodEnum {
    card = "card",
    cash = "cash",
    wallet = "wallet",
    ThirdParty = "ThirdParty"
}

export enum paymentStatusEnum {
    pending = "pending",
    completed = "completed",
    failed = "failed",
    refunded = "refunded"
}